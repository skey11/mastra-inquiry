diff --git a/node_modules/@mastra/deployer-cloudflare/dist/index.cjs b/node_modules/@mastra/deployer-cloudflare/dist/index.cjs
index 1111111..2222222 100644
--- a/node_modules/@mastra/deployer-cloudflare/dist/index.cjs
+++ b/node_modules/@mastra/deployer-cloudflare/dist/index.cjs
@@
-    import { checkEvalStorageFields } from '@mastra/core/utils';
+    import { checkEvalStorageFields } from '@mastra/core/utils';
+
+    const STATIC_ALLOWED_ORIGINS = [
+      "https://449cdfa5.mastra-frontend.pages.dev",
+      "https://mastra.viplook.dpdns.org",
+    ];
+
+    const CORS_BASE_HEADERS = {
+      "Access-Control-Allow-Methods": "GET, POST, PUT, PATCH, DELETE, OPTIONS",
+      "Access-Control-Allow-Headers": "Content-Type, Authorization, x-mastra-client-type",
+      "Access-Control-Allow-Credentials": "true",
+    };
+
+    const parseEnvOrigins = (env) => {
+      const envOrigins = env?.CORS_ALLOWED_ORIGINS
+        ? env.CORS_ALLOWED_ORIGINS.split(',').map((origin) => origin.trim()).filter(Boolean)
+        : [];
+      const frontendOrigin = env?.FRONTEND_ORIGIN ? [env.FRONTEND_ORIGIN] : [];
+      return [...STATIC_ALLOWED_ORIGINS, ...envOrigins, ...frontendOrigin].filter(Boolean);
+    };
+
+    const resolveAllowOrigin = (origin, env) => {
+      if (!origin) return null;
+      const allowedOrigins = parseEnvOrigins(env);
+      if (allowedOrigins.includes('*')) {
+        return origin;
+      }
+      return allowedOrigins.includes(origin) ? origin : null;
+    };
+
+    const applyCorsHeaders = (response, requestOrigin, env) => {
+      const allowOrigin = resolveAllowOrigin(requestOrigin, env);
+      if (!allowOrigin) {
+        return response;
+      }
+      const headers = new Headers(response.headers);
+      headers.set('Access-Control-Allow-Origin', allowOrigin);
+      headers.append('Vary', 'Origin');
+      Object.entries(CORS_BASE_HEADERS).forEach(([key, value]) => {
+        headers.set(key, value);
+      });
+      return new Response(response.body, {
+        status: response.status,
+        statusText: response.statusText,
+        headers,
+      });
+    };
+
+    const buildPreflightResponse = (requestOrigin, env) => {
+      const headers = new Headers(CORS_BASE_HEADERS);
+      const allowOrigin = resolveAllowOrigin(requestOrigin, env);
+      if (allowOrigin) {
+        headers.set('Access-Control-Allow-Origin', allowOrigin);
+      }
+      headers.append('Vary', 'Origin');
+      return new Response(null, { status: 204, headers });
+    };
@@
-        const app = await createHonoServer(_mastra, { tools: getToolExports(tools) });
-        return app.fetch(request, env, context);
+        const app = await createHonoServer(_mastra, { tools: getToolExports(tools) });
+        const origin = request.headers.get('origin') ?? '';
+        if (request.method === 'OPTIONS') {
+          return buildPreflightResponse(origin, env);
+        }
+        const response = await app.fetch(request, env, context);
+        return applyCorsHeaders(response, origin, env);
       }
     }
 `;
diff --git a/node_modules/@mastra/deployer-cloudflare/dist/index.js b/node_modules/@mastra/deployer-cloudflare/dist/index.js
index 1111111..2222222 100644
--- a/node_modules/@mastra/deployer-cloudflare/dist/index.js
+++ b/node_modules/@mastra/deployer-cloudflare/dist/index.js
@@
-    import { checkEvalStorageFields } from '@mastra/core/utils';
+    import { checkEvalStorageFields } from '@mastra/core/utils';
+
+    const STATIC_ALLOWED_ORIGINS = [
+      "https://449cdfa5.mastra-frontend.pages.dev",
+      "https://mastra.viplook.dpdns.org",
+    ];
+
+    const CORS_BASE_HEADERS = {
+      "Access-Control-Allow-Methods": "GET, POST, PUT, PATCH, DELETE, OPTIONS",
+      "Access-Control-Allow-Headers": "Content-Type, Authorization, x-mastra-client-type",
+      "Access-Control-Allow-Credentials": "true",
+    };
+
+    const parseEnvOrigins = (env) => {
+      const envOrigins = env?.CORS_ALLOWED_ORIGINS
+        ? env.CORS_ALLOWED_ORIGINS.split(',').map((origin) => origin.trim()).filter(Boolean)
+        : [];
+      const frontendOrigin = env?.FRONTEND_ORIGIN ? [env.FRONTEND_ORIGIN] : [];
+      return [...STATIC_ALLOWED_ORIGINS, ...envOrigins, ...frontendOrigin].filter(Boolean);
+    };
+
+    const resolveAllowOrigin = (origin, env) => {
+      if (!origin) return null;
+      const allowedOrigins = parseEnvOrigins(env);
+      if (allowedOrigins.includes('*')) {
+        return origin;
+      }
+      return allowedOrigins.includes(origin) ? origin : null;
+    };
+
+    const applyCorsHeaders = (response, requestOrigin, env) => {
+      const allowOrigin = resolveAllowOrigin(requestOrigin, env);
+      if (!allowOrigin) {
+        return response;
+      }
+      const headers = new Headers(response.headers);
+      headers.set('Access-Control-Allow-Origin', allowOrigin);
+      headers.append('Vary', 'Origin');
+      Object.entries(CORS_BASE_HEADERS).forEach(([key, value]) => {
+        headers.set(key, value);
+      });
+      return new Response(response.body, {
+        status: response.status,
+        statusText: response.statusText,
+        headers,
+      });
+    };
+
+    const buildPreflightResponse = (requestOrigin, env) => {
+      const headers = new Headers(CORS_BASE_HEADERS);
+      const allowOrigin = resolveAllowOrigin(requestOrigin, env);
+      if (allowOrigin) {
+        headers.set('Access-Control-Allow-Origin', allowOrigin);
+      }
+      headers.append('Vary', 'Origin');
+      return new Response(null, { status: 204, headers });
+    };
@@
-        const app = await createHonoServer(_mastra, { tools: getToolExports(tools) });
-        return app.fetch(request, env, context);
+        const app = await createHonoServer(_mastra, { tools: getToolExports(tools) });
+        const origin = request.headers.get('origin') ?? '';
+        if (request.method === 'OPTIONS') {
+          return buildPreflightResponse(origin, env);
+        }
+        const response = await app.fetch(request, env, context);
+        return applyCorsHeaders(response, origin, env);
       }
     }
 `;
@@
